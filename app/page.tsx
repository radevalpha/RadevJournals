# Clone the repository
git clone https://github.com/radevalpha/RadevJournals.git
cd RadevJournals

# Create directories
mkdir -p app

# Create files (copy and paste the code from above)
# Save each file in its proper location

# Add, commit, and push
git add .
git commit -m "Add Next.js project files"
git push
